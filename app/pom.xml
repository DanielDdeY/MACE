<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.mace</groupId>
    <artifactId>mace-app</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>MACE (CorePulse)</name>
    <description>
        Sistema de telemetria de hardware pasivo y gestor de procesos interactivo para Windows,
        con arquitectura hexagonal (dominio Java puro + puente FFM + UI JavaFX).
    </description>

    <properties>
        <!-- Base de compilacion -->
        <maven.compiler.release>22</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Dev 3 (FFM / Project Panama) necesita enable-preview para compilar y correr -->
        <enable.preview.arg>--enable-preview</enable.preview.arg>

        <!-- Versiones centralizadas: si alguien necesita subir una version, cambiar aqui -->
        <javafx.version>22.0.2</javafx.version>
        <atlantafx.version>2.0.1</atlantafx.version>
        <junit.version>5.10.2</junit.version>
        <mockito.version>5.12.0</mockito.version>

        <!-- Dev 4: clase principal de arranque de la app JavaFX -->
        <app.mainClass>com.mace.presentation.MaceApplication</app.mainClass>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>

        <!-- ==================== DEV 4: UI (JavaFX + AtlantaFX) ==================== -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.mkpaz</groupId>
            <artifactId>atlantafx-base</artifactId>
            <version>${atlantafx.version}</version>
        </dependency>

        <!-- ==================== DEV 2: Dominio / Casos de uso (tests) ==================== -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>

        <!--
            DEV 1 (C nativo): no requiere dependencias Maven, vive fuera de este pom (native/, CMake).
            DEV 3 (FFM / Panama): usa java.lang.foreign.* del propio JDK 22+, tampoco requiere
            dependencia externa; solo necesita enable-preview, ya configurado abajo en los plugins.
        -->

    </dependencies>

    <build>
        <plugins>

            <!-- Compilacion con soporte enable-preview para el puente FFM de Dev 3 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>22</release>
                    <compilerArgs>
                        <arg>${enable.preview.arg}</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <!-- Tests con enable-preview tambien, por si Dev 2/Dev 3 testean codigo FFM -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <argLine>--enable-preview</argLine>
                </configuration>
            </plugin>

            <!-- Permite correr la UI con: mvn javafx:run -->
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>${app.mainClass}</mainClass>
                    <options>
                        <option>--enable-preview</option>
                    </options>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>